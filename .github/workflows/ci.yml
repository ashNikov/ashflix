name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Placeholder frontend checks
        run: echo "Frontend checks will go here"

      - name: Placeholder backend checks
        run: echo "Backend checks will go here"

      - name: Placeholder security scans
        run: echo "Security scans will go here"
